package re._1r.euler.problems

object E101 {
/*  def ddx(ar: Array[Int]): Array[Int] = {
    ar.zip(ar.drop(1)).map(x => {
      x._2 - x._1
    })
  }

  def find(ar: Array[Int], x: Array[Int]): Array[Int] = {
    println(ar.mkString("|"))
    ddx(ar) match {
      case x if x.length <= 1 || x.forall(_ == x.head) => Array(x.headOption.getOrElse(0))
      case x => find(x) :+ x.headOption.getOrElse(0)
    }
  }

  def result = {
    val x = Array(1, 8, 27, 64, 125, 216, 343)
    find(x).mkString(",")
  }*/
}